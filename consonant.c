#include<stdio.h>
void main()
{
char alpha;
scanf("%c",&alpha);
if((alpha=='a')||(alpha=='e')||(alpha=='i')||(alpha=='o')||(alpha=='u')||(alpha=='A')||(alpha=='E')||I||O||U)))
    printf("%c is a vowel",alpha);
else
    printf("%c is a consonant",alpha);
}
