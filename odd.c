#include<stdio.h>
void main()
{
int number;
scanf("%d",&number);
if((n>1)&&(number!='\0'))
{
if(number%2==0)
printf("number%d is even",number);
else
printf("number%d is odd",number);
}
else
  printf("not applicable");
}
